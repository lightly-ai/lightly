[tool.black]
extend-exclude = """
(
    lightly/openapi_generated/.*
    | lightly/openapi_client/.*
)
"""

[tool.isort]
profile = "black"
extend_skip_glob = ["lightly/openapi_generated", "lightly/openapi_client"]
